# Test status

## Tested default execution

The canonical Section 1 notebook was executed from its first cell to its final cell with `RUN_MODE = "MANUSCRIPT_SNAPSHOT"` and the packaged companion ZIP.

- Notebook execution errors: **0**
- Local execution time: approximately **11 seconds**
- Input checksum checks: **39/39 passed**
- Frozen output comparisons: **11/11 passed**
- Fixed numerical checks: **15/15 passed**
- Section 2 bridge checks: **22/22 passed**
- Success marker: `PIPELINE_SUCCESS.txt` created

## Repeated-run determinism

Two fresh notebook executions produced:

- the same output file set;
- byte-identical scientific tables, figures, metadata exports, audit tables, and success marker after excluding the two deliberately runtime-specific text files (`00_run_manifest.json` and `UPSTREAM_REPRODUCIBILITY_REPORT.md`); and
- a byte-identical `Mushroom_KG_Reproducibility_Inputs_v2_from_upstream.zip`.

## Complete-chain validation

The bridge ZIP generated by Section 1 was extracted and passed directly to the existing Section 2 `pipeline_core.py`.

- Section 2 execution errors: **0**
- Section 2 input/preflight checks: passed
- Section 2 expected-result checks: **94/94 passed**
- Section 2 success marker: created

## Live-code smoke tests

The external database calls were not executed because no user API credentials were used during package construction. The following non-network live components were tested with synthetic records:

- PubMed/Scopus harmonization and DOI/title deduplication;
- deterministic live sentence segmentation and entity extraction;
- typed relation extraction and co-occurrence generation;
- application of an expert topic-review file;
- automatic exclusion of a retained BERTopic outlier topic `-1`; and
- propagation of expert-consolidated themes into the live semantic-extraction tables.

All smoke tests passed.

## Tested environment

- Python 3.13.5
- pandas 2.2.3
- NumPy 2.3.5
- spaCy 3.8.11
- SciPy 1.17.0
- Matplotlib 3.10.8

The notebook uses compatible version ranges and avoids downloading a spaCy language model in snapshot mode. Exact result audits and the full Section 2 fixed-result suite provide safeguards against environment-related drift.

## Upload-ready release validation

The repository-specific Colab badge and automatic companion-ZIP URL were
updated for `AISKG_01_Framework`. The updated notebook was executed from the
first code cell through the final code cell in an isolated working directory.
It completed with zero cell errors and regenerated the deterministic reference
output archive included in `reference_outputs/`.

## Upload-ready release validation

The repository-specific Colab badge and automatic companion-ZIP URL were
updated for `AISKG_01_Framework`. The updated notebook was executed from the
first code cell through the final code cell in an isolated working directory.
It completed with zero cell errors and regenerated the deterministic reference
output archive included in `reference_outputs/`.

